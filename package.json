{
  "name": "trivy-exporter",
  "version": "1.1.1",
  "type": "module",
  "scripts": {
    "dev": "CRON_RULE=\"0 * * * * *\" node --watch index.js",
    "lint": "eslint .",
    "test": "NODE_ENV=test node --disable-warning=ExperimentalWarning --test-force-exit --test-concurrency=1 --test test/*.js"
  },
  "license": "MIT",
  "dependencies": {
    "@swimlane/docker-reference": "^2.0.1",
    "node-cron": "^3.0.2",
    "prom-client": "^14.2.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@types/node-cron": "^3.0.11",
    "clean-modules": "^3.1.1",
    "eslint": "^9.39.1",
    "neostandard": "^0.12.2",
    "typescript": "^5.9.3"
  }
}
